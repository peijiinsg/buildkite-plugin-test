steps:
  - label: ':hammer: Tests'
    command: 'echo "hello world!" && export PACT_RAN=true && export TEST_STATUS=failed'
    env:
      BUILDKITE_DOCKER_COMPOSE_CONTAINER: app
    plugins:
      - github.latitudefinancial.com/Latitude/datadog-metrics-buildkite-plugin#v1.0.0:
        metric: pact.contract.verified.$TEST_STATUS
        api_key: 342423423423
        tag:
          - "pacticipant:ProductService"
          - "domain:servicing"
          - "branch:master"
          - "version:b43bEVWQv"
        type: count
        value: 1
        condition: $PACT_RAN